exports.sayHello = (req, res) => {
    const html = `
          <!DOCTYPE html>
          <html lang="en">
          <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <title>YangJae</title>
              <style>
                  body {
                      font-family: 'Arial', sans-serif;
                      display: flex;
                      justify-content: center;
                      align-items: center;
                      height: 100vh;
                      margin: 0;
                      background: linear-gradient(135deg, #000428, #004e92);
                      perspective: 1500px;
                      overflow: hidden;
                      position: relative;
                  }
                  .stars {
                      position: absolute;
                      top: 0;
                      left: 0;
                      width: 100%;
                      height: 100%;
                      background: url('https://cms-imgp.jw-cdn.org/img/p/202018410/univ/art/202018410_univ_lsr_lg.jpg') center center/cover no-repeat; /* 배경 이미지를 중앙으로 고정 */
                      filter: blur(2px) brightness(0.6); /* 이미지를 약간 흐릿하고 어둡게 처리하여 텍스트를 돋보이게 */
                      animation: twinkle 4s ease-in-out infinite;
                      z-index: 0;
                  }
                  .container {
                      text-align: center;
                      padding: 60px;
                      background: rgba(255, 255, 255, 0.15);
                      border-radius: 20px;
                      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.6);
                      transform: rotateY(20deg) rotateX(10deg);
                      animation: float 8s ease-in-out infinite;
                      z-index: 1;
                      backdrop-filter: blur(10px); /* 컨테이너에 배경 흐림 효과 추가 */
                  }
                  h1 {
                      color: #f8f8f8;
                      font-size: 2.5em;
                      text-shadow: 0 0 20px rgba(255, 255, 255, 0.8);
                      animation: glow 2s ease-in-out infinite alternate;
                      margin: 0;
                  }
                  p {
                      color: #eeeeee;
                      font-size: 1.4em;
                      font-style: italic;
                      text-shadow: 0 0 10px rgba(255, 255, 255, 0.6);
                      margin-top: 10px;
                  }
                  @keyframes float {
                      0% { transform: rotateY(20deg) rotateX(10deg) translateY(0); }
                      50% { transform: rotateY(20deg) rotateX(10deg) translateY(-10px); }
                      100% { transform: rotateY(20deg) rotateX(10deg) translateY(0); }
                  }
                  @keyframes glow {
                      0% { text-shadow: 0 0 20px rgba(255, 255, 255, 0.8); }
                      100% { text-shadow: 0 0 30px rgba(255, 255, 255, 1); }
                  }
                  @keyframes twinkle {
                      0%, 100% { opacity: 0.8; }
                      50% { opacity: 1; }
                  }
              </style>
          </head>
          <body>
              <div class="stars"></div> <!-- 배경 이미지 -->
              <div class="container">
                  <h1>YangJae Cart Witnessing</h1>
                  <p>Ac 5:19~21,42</p>
              </div>
          </body>
          </html>
      `;
  
    res.send(html);
};