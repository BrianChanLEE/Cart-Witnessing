


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>스케줄 목록</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #000428, #004e92);
            perspective: 1500px;
            overflow: hidden;
            position: relative;
        }
        .stars {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: transparent url('https://cms-imgp.jw-cdn.org/img/p/202018410/univ/art/202018410_univ_lsr_lg.jpg') center center/cover no-repeat;
            filter: blur(2px) brightness(0.6);
            animation: twinkle 4s ease-in-out infinite;
            z-index: 0;
        }
        .container {
            text-align: center;
            padding: 60px;
            background: rgba(255, 255, 255, 0.15);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.6);
            z-index: 1;
            backdrop-filter: blur(10px);
            transform: rotateY(20deg) rotateX(10deg);
            animation: float 8s ease-in-out infinite;
            width: 800px;
            max-width: 100%;
        }
        h1 {
            color: #f8f8f8;
            font-size: 2.5em;
            text-shadow: 0 0 20px rgba(255, 255, 255, 0.8);
            margin: 0;
        }
        .schedule-container {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 20px;
        }
        .schedule-item {
            flex: 1 1 33.33%;
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid #ccc;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }
        .schedule-item h2 {
            margin: 0;
            font-size: 1.5em;
            color: #fff;
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.6);
        }
        .schedule-item p {
            margin: 10px 0;
            color: #eee;
            font-size: 1.1em;
        }
        .schedule-add {
            margin-top: 20px;
        }
        .schedule-add a {
            color: #fff;
            text-decoration: none;
            background-color: #007bff;
            padding: 10px 20px;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }
        .schedule-add a:hover {
            background-color: #0056b3;
        }

        /* 작은 화면에서 2개씩 배치 */
        @media (max-width: 800px) {
            .schedule-item {
                flex: 1 1 50%;
            }
        }

        /* 더 작은 화면에서 1개씩 배치 */
        @media (max-width: 500px) {
            .schedule-item {
                flex: 1 1 100%;
            }
        }
        @keyframes float {
            0% { transform: rotateY(20deg) rotateX(10deg) translateY(0); }
            50% { transform: rotateY(20deg) rotateX(10deg) translateY(-10px); }
            100% { transform: rotateY(20deg) rotateX(10deg) translateY(0); }
        }
        @keyframes twinkle {
            0%, 100% { opacity: 0.8; }
            50% { opacity: 1; }
        }
    </style>
</head>
<body>
    <div class="stars"></div>
    <div class="container">
        <h1>스케줄 목록</h1>
        <div class="schedule-container">
            <% schedules.forEach(scheduleGroup => { %>
                <div class="schedule-item">
                    <h2>장소: <%= scheduleGroup.location %></h2>
                    <% if (Array.isArray(scheduleGroup.schedule.schedule)) { %>
                        <% scheduleGroup.schedule.schedule.forEach(entry => { %>
                            <p><%= entry.time %> - <%= entry.people.join(', ') %></p>
                        <% }); %>
                    <% } else { %>
                        <p>스케줄 데이터가 없습니다.</p>
                    <% } %>
                </div>
            <% }); %>
        </div>

        <div class="schedule-add">
            <a href="/add-schedule">스케줄 추가하기</a>
            <a href="/edit-schedule">업데이트</a>
        </div>
    </div>
</body>
</html>