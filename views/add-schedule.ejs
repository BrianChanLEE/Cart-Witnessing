 <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>스케줄 입력</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #000428, #004e92);
            perspective: 1500px;
            overflow: hidden;
            position: relative;
        }
        .stars {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: transparent url('https://cms-imgp.jw-cdn.org/img/p/202018410/univ/art/202018410_univ_lsr_lg.jpg') center center/cover no-repeat;
            filter: blur(2px) brightness(0.6);
            animation: twinkle 4s ease-in-out infinite;
            z-index: 0;
        }
        .container {
            text-align: center;
            padding: 60px;
            background: rgba(255, 255, 255, 0.15);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.6);
            z-index: 1;
            backdrop-filter: blur(10px);
            transform: rotateY(20deg) rotateX(10deg);
            animation: float 8s ease-in-out infinite;
            width: 400px;
        }
        h1 {
            color: #f8f8f8;
            font-size: 2.5em;
            text-shadow: 0 0 20px rgba(255, 255, 255, 0.8);
            margin: 0;
        }
        form {
            margin-top: 20px;
        }
        label {
            color: #eeeeee;
            font-size: 1.2em;
            display: block;
            margin-bottom: 8px;
        }
        input[type="text"], input[type="time"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        button {
            padding: 10px 20px;
            font-size: 1.2em;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }
        button:hover {
            background-color: #0056b3;
        }
        a {
            color: #fff;
            text-decoration: none;
            display: block;
            margin-top: 20px;
        }
        a:hover {
            text-decoration: underline;
        }
        @keyframes float {
            0% { transform: rotateY(20deg) rotateX(10deg) translateY(0); }
            50% { transform: rotateY(20deg) rotateX(10deg) translateY(-10px); }
            100% { transform: rotateY(20deg) rotateX(10deg) translateY(0); }
        }
        @keyframes twinkle {
            0%, 100% { opacity: 0.8; }
            50% { opacity: 1; }
        }
    </style>
</head>
<body>
    <div class="stars"></div>
    <div class="container">
        <h1>스케줄 입력</h1>
        <form action="/add-schedule" method="POST">
            <label for="location">위치:</label>
            <input type="text" id="location" name="location" required>

            <label for="startTime">시작 시간 (24시간 형식):</label>
            <input type="time" id="startTime" name="startTime" required>

            <label for="people">사람명 (쉼표로 구분):</label>
            <input type="text" id="people" name="people" required>

            <button type="submit">추가</button>
        </form>

        <a href="/schedules">스케줄 목록 보기</a>
    </div>
</body>
</html> 


